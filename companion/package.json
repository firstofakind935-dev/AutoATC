{
  "name": "autoatc-companion",
  "version": "0.1.0",
  "private": true,
  "description": "Screen-tracking companion app - reads a pilot's own PTFS window (HUD text + minimap) and reports an estimated position to the AutoATC monitor service.",
  "main": "main.js",
  "scripts": {
    "start": "electron ."
  },
  "dependencies": {
    "tesseract.js": "^5.1.1"
  },
  "devDependencies": {
    "electron": "^32.0.0"
  }
}
